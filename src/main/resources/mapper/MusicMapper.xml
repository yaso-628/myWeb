<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myweb.mapper.MusicMapper">
    <resultMap id="BaseResultMap" type="com.myweb.model.Music">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="artist" property="artist"/>
        <result column="url" property="url"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <sql id="Base_Column_List">id, title, artist, url, sort, create_time, update_time</sql>
    <select id="selectById" resultMap="BaseResultMap">SELECT <include refid="Base_Column_List"/> FROM music WHERE id = #{id}</select>
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM music ORDER BY sort ASC, id ASC
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO music (title, artist, url, sort, create_time, update_time)
        VALUES (#{title}, #{artist}, #{url}, #{sort}, #{createTime}, #{updateTime})
    </insert>
    <update id="updateById">
        UPDATE music SET title=#{title}, artist=#{artist}, url=#{url}, sort=#{sort}, update_time=#{updateTime} WHERE id=#{id}
    </update>
    <delete id="deleteById">DELETE FROM music WHERE id = #{id}</delete>
</mapper>
