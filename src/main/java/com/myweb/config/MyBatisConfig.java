package com.myweb.config;

import org.mybatis.spring.annotation.MapperScan;
import org.springframework.context.annotation.Configuration;

/**
 * MyBatis 配置（扫描已放在启动类，此处可扩展 TypeHandler、分页等）
 */
@Configuration
public class MyBatisConfig {
}
